development:
  clients:
    default:
      host: localhost
      database: tramp_development

test:
  clients:
    default:
      host: localhost
      database: tramp_test

production:
  clients:
    default:
      uri: <%= ENV['MONGODB_URL'] %>
